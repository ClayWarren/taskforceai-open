import type { ConversationRecord, MessageRecord } from '@taskforceai/client-runtime';

/** Persisted conversation shapes owned by mobile conversation storage. */
export type LocalConversation = ConversationRecord;
export type LocalMessage = MessageRecord;
