TaskForceAI locked computer use packages its macOS authorization plug-in in this directory.

The desktop app exposes status and enablement commands now. A signed
TaskForceAILockedComputerUse.bundle belongs here when the privileged installer
is wired into the macOS release flow.
