# TypeScript Core

Home for TypeScript product rules shared by web, mobile, desktop, and package
adapters.

Keep code here independent of UI frameworks, persistence adapters, telemetry
providers, generated wire clients, and concrete host details. The current
`client-core` package is still transitional: pure client rules belong here, while
browser/runtime/storage/logging details are candidates for adapters or
infrastructure splits.
