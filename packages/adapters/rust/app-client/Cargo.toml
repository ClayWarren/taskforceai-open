[package]
name = "taskforceai-app-client"
version = "0.1.0"
edition = "2021"
description = "TaskForceAI app-server client"
authors = ["TaskForceAI"]
license = "MIT"
repository = "https://github.com/TaskForceAI"
rust-version = "1.96.0"
publish = false

[dependencies]
futures-util = "0.3"
log = "0.4"
minisign-verify = "0.2.5"
rand = "0.9"
semver = "1.0"
serde = {version = "1.0.228", features = ["derive"]}
serde_json = "1.0.145"
sha2 = "0.10"
taskforceai-core = {path = "../../../core/rust"}
taskforceai-app-protocol = {path = "../../../contracts/rust"}
thiserror = "1.0"
reqwest = {version = "0.12", features = ["json", "rustls-tls", "stream"], default-features = false}
tokio = {version = "1.0", features = ["fs", "io-util", "process", "sync", "rt", "macros", "time"]}
tracing = "0.1"
url = "2.5"

[dev-dependencies]
tokio = {version = "1.0", features = ["io-util", "process", "sync", "rt", "macros", "time", "net"]}
